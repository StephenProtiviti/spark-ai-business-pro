Remove the white background from `src/assets/spark-logo.png` using the `imagegen--edit_image` tool with `transparent_background: true`, saving the result back to the same file path as a transparent PNG. The white rounded containers in the navbar and homepage hero will be kept as-is per user preference.